# OpenAI API Key
OPENAI_API_KEY=sk-...

# Authentifizierung
APP_PASSWORD=sicheres-passwort-hier
JWT_SECRET=zufaelliger-string-fuer-token-signatur

# Standard-Einstellungen (werden in DB gespeichert und können zur Laufzeit geändert werden)
DEFAULT_MODEL=gpt-4o-mini
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
