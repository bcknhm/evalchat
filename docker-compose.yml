services:
  app:
    build: .
    container_name: evalchat
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    env_file:
      - .env
    networks:
      - proxy

networks:
  proxy:
    external: true
